# Flask REST API

REST API built with Flask for CRUD operations with SQLite database.

## Setup

```bash
pip install -r requirements.txt
python main.py
```

## Stack

Python · Flask · SQLite
